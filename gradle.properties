# Container engine selection for './gradlew all' task
# Values: "auto" (auto-detect), "docker", or "podman"
# Default: "auto" — detects based on available commands (prefers podman-compose)
container.engine=auto
